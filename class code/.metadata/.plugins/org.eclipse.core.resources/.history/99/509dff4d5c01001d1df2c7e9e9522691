List<Integer> ints = new ArrayList<>();
ints.add(1);
ints.add(2);
List<Number> nums = ints; // Compile time error due to variable type mismatch
// Number is super class of Integer but List<Integer> is not subtype of List<Number>
nums.add(3.14);
